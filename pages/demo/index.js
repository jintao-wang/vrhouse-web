import React from 'react';
import styled from 'styled-components';

const ContainerSC = styled('div')`
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  background: #2B2B2B;
`;

export default () => (
  <ContainerSC />
);
